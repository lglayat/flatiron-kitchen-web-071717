<%= form_for @recipe do |f| %>
  <%= f.label :ingredients, 'Name:' %><br>
  <%= f.text_field :name %><br>

  <%= f.label :ingredients, 'Ingredients:' %><br>
  <%= f.collection_check_boxes :ingredient_ids, Ingredient.all, :id, :name %>

  <%= f.submit %>
<% end %>
